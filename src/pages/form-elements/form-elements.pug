include ../../blocks/text-field/text-field

<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Form elements
    body
        +text-field({hasHeaderDescription: true,
                    hasHeader: true,
                    description: 'default', 
                    header: 'text field', 
                    placeholder: 'email', 
                    condition: '',
                    value: '',
                    classes: ''})  