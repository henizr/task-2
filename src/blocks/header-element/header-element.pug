//-

  Опции миксина header-element:

     options.hasHeaderDescription - Наличие описания (boolean)
     options.headerDescription - Текст описания (string)
     options.header - Текст заголовка (string)

    Пример использования:

        +header-element( { hasHeaderDescription: true, headerDescription: 'default', header: 'text field' } )


mixin header-element(options)
    .header-element
        h3(class="header-element__header") #{options.header}
        if options.hasHeaderDescription 
            p(class="header-element__description") #{options.headerDescription}